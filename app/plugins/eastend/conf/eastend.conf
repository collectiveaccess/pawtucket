# Set to 0 to disable the EastEnd plugin
enabled = 0

# navigation.conf menu code to place EastEnd menu items *before*
# Leave this blank if you want this to be the last (right most) item in the top-level navigation bar
position_top_level_nav_item_before = about

featured_artists_set_name = featuredArtists

participate_set_name = participate

artist_browser_place_ids = [40, 186, 5, 74, 20, 21]