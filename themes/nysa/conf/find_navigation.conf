#
# Defines navigation back to results for various types of "find" operations
#
ca_objects = {
	basic_search => {
		module_path = ,
		controller = Search,
		action = getReturnToResultsUrl
	},
	advanced_search => {
		module_path = ,
		controller = AdvancedSearch,
		action = getReturnToResultsUrl
	},
	basic_browse => {
		module_path = ,
		controller = Browse,
		action = getReturnToResultsUrl
	},
	simple_gallery => {
		module_path = simpleGallery,
		controller = Show,
		action = getReturnToResultsUrl
	},
	sets => {
		module_path = ,
		controller = Sets,
		action = getReturnToResultsUrl
	}
}

ca_entities = {
	basic_search => {
		module_path = ,
		controller = Search,
		action = getReturnToResultsUrl
	},
	advanced_search => {
		module_path = ,
		controller = AdvancedSearch,
		action = getReturnToResultsUrl
	},
	basic_browse => {
		module_path = ,
		controller = Browse,
		action = getReturnToResultsUrl
	}
}

ca_places = {
	basic_search => {
		module_path = ,
		controller = Search,
		action = getReturnToResultsUrl
	},
	advanced_search => {
		module_path = ,
		controller = AdvancedSearch,
		action = getReturnToResultsUrl
	},
	basic_browse => {
		module_path = ,
		controller = Browse,
		action = getReturnToResultsUrl
	}
}

ca_occurrences = {
	basic_search => {
		module_path = ,
		controller = Search,
		action = getReturnToResultsUrl
	},
	advanced_search => {
		module_path = ,
		controller = AdvancedSearch,
		action = getReturnToResultsUrl
	},
	basic_browse => {
		module_path = ,
		controller = Browse,
		action = getReturnToResultsUrl
	}
}

ca_collections = {
	basic_search => {
		module_path = ,
		controller = Search,
		action = getReturnToResultsUrl
	},
	advanced_search => {
		module_path = ,
		controller = AdvancedSearch,
		action = getReturnToResultsUrl
	},
	basic_browse => {
		module_path = ,
		controller = Browse,
		action = getReturnToResultsUrl
	}
}